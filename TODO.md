# TODO

## Features

- [ ] добавить маску и валидацию для инпутов категорий (exercise-categories-list, service-categories-list)
- [ ] ? добавить id категории в списках категорий и (в случае если категория присутствует в фиче) в фичах сущностей к ним относящихся (exercise-categories-list, exercises-list, service-categories-list, services-list)
- [ ] ? добавить количество сущностей относящихся к категории в заголовок аккордеона в списках сущностей и выключать опцию раскрытия при отсутствии (exercise-categories-list, service-categories-list)
- [x] доперевести ui editor.js на русский
- [x] дооверрайдить стили editor.js под темную тему
- [ ] сделать предупреждение при переходе со страницы с заполненными данными
- [x] глобальный обработчик для неперехваченных ошибок ? - пока нет
- [x] кажется существует проблема с работой плагинов списков editor.js - конкретно они не могут распарсить их из сохраненных данных
      DONE: проблема была в передаче реактивного объекта в свойство data конфига editor.js
- [x] прерывание запросов при уходе со страницы (размонтировании компонента)

## Refactor

- [ ] очень большое количество дублирующегося кода в компонентах, стоит выделить повторяющийся функционал в отдельные фичи и/или `composables`/`utils`/`shared-ui`
      - [x] entities layer
      - [ ] lists?
- [x] кажется можно для слоя бизнес-сущностей сделать фабрики для апи и сторов
- [x] вынести разворачивание и подготовку данных ответа апи на уровень стора везде
- [x] исправить неверный порядок обновления локальных данных относительно запросов на сервер в сторах
- [x] проверить необходимость нормализации в компоузеблах, при необходимости - добавить
- [x] проинспектировать обработку ошибок, в частности смущает прокидывание ошибки выше во многих местах, когда ошибка уже обработана
      хотя это может пригодиться для логгирования и анализа ошибок в будущем
- [x] try/catch
- [x] исправить прямое изменение данных стора в некоторых фичах - e.g. PricesList
- [x] По хорошему сделать коллекции кодов ошибок, пошарить их общим npm-пакетом между фронтом и бэком и завести i18n словарь на фронте, в котором будем хранить человекочитаемые сообщения для ошибок на разных языках, вместо того, чтобы на бэке их прописывать
- [ ] PUT семантически неверно используется, на деле у нас PATCH под маской PUT
- [x] Изменить логику вычисления `isLoading` флага в `CrudComposable` - сейчас если выполняется несколько запросов одновременно, то первый выполнившийся сбрасывает флаг

## Build

- [ ] уменьшить размер бандла и максимальный размер отдельных файлов?

## Architecture

- [ ] унифицировать размеры сущностей, создаваемых в редакторе editor.js между админкой и клиентом,
      и задокументировать это решение
      варианты:
      - npm пакет?
      - формальный контракт, но 2 источника?